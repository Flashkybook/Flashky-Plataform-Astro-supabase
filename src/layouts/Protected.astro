---
import type { SPB_UserData } from "@/env";
const { cookies, redirect, url } = Astro;

const user_cookie = cookies.get("user")?.value;
if (user_cookie === undefined ) {
	return redirect("/");
}

const user_data:SPB_UserData = user_cookie && JSON.parse(user_cookie as string);
---

<slot/>