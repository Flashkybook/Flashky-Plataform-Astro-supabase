---
import Layout from "@layout";
import Study from "@app/session/components";
import { getUserSsr } from "@app/user/auth/controllers";

const userdata = await getUserSsr(Astro);

---
<Layout userdata={userdata}>
    <Study client:only="preact" />
</Layout>
